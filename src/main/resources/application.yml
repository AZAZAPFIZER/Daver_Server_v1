server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/dodamdodam_v6?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: 123456789

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
      show_sql: false
      format_sql: true

logging.level:
  org.springframework.boot.autoconfigure: ERROR